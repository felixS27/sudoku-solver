# sudoku-solver

# ToDO:

- restrcuture the notebook into .py files
- Add an GUI interface for easier input and output
- make it possible to load csv files into the GUI and modify mistakes
- make it possible to export it as csv file